- var svgSprite = 'img/sprite.svg'

mixin icon(name, modClass)
  if modClass == undefined
    - var modClass = ''
  else
    - var modClass = ' ' + modClass
  svg(class=name + modClass)
    use(xlink:href= svgSprite + '#' + name)


mixin breadcrumbs(items)
  .breadcrumbs
    .container
      ul
        each item in items
          li
            a(href="#")= item
              i.icon.icon-right-arrow-1